# 搜索策略优化总结

## 修改日期
2025-01-XX

## 优化目标
1. 解决搜索粒度太细的问题（按章节搜导致找不到宽泛的 Playlist）
2. 解决 Tavily 返回单视频被过滤的问题

## 主要修改

### 1. QueryGenerator 类 - 搜索词生成策略优化

#### 修改位置
`search_strategist.py` - `QueryGenerator.generate_initial_query()` 方法

#### 核心变更
**旧策略**：按章节生成搜索词，粒度太细
**新策略**：更具"聚合性"的搜索词，强制限定站点

#### 新的搜索策略（5种模式）

**尝试 1（精准列表）**：
```
格式: "site:youtube.com playlist matematika {grade_level} {chapter_title}"
目的: 尝试找具体章节的列表，但限定在 YouTube
```

**尝试 2（学期/全套合集 - 核心修改）**：
```
格式: "site:youtube.com playlist matematika {grade_level} full course semester 1/2"
关键: 去掉了章节名，只保留年级和学科！
目的: 找到整个学期的完整课程合集
```

**尝试 3（寻找频道）**：
```
格式: "rekomendasi channel youtube belajar matematika {grade_level} terbaik"
目的: 找出好的教育频道
```

**尝试 4（宽泛搜索）**：
```
格式: "playlist lengkap matematika {grade_level} {semester}"
目的: 不限定站点，搜索学期级别的完整播放列表
```

**尝试 5（教育平台）**：
```
格式: "ruangguru zenius pahamify matematika {grade_level} {chapter}"
目的: 搜索多个教育平台
```

#### 新增功能
- `_determine_semester()` 方法：自动判断章节属于哪个学期
  - 基于章节名称关键词（geometri, statistik, peluang 等）
  - 默认返回 "semester 1"

#### 日志增强
- 每个尝试都会打印详细的查询生成信息
- 显示查询模式、限定站点、学期等信息

---

### 2. ResultInspector 类 - 评估标准放宽

#### 修改位置
`search_strategist.py` - `ResultInspector.evaluate_results()` 方法

#### 核心变更
**旧标准**：严格要求播放列表 URL（包含 "playlist" 或 "list="）
**新标准**：放宽标准，允许单集视频和系列视频的一部分

#### 新的评估标准（更新到 Prompt）

1. **允许单集视频**：
   - 如果 URL 是 `youtube.com/watch` 但标题包含以下关键词，标记为合格：
     * "Full", "Lengkap", "Complete"
     * "Part 1", "Part 2", "Bagian 1", "Bagian 2"
     * "Series", "Seri", "Episode"
     * "Playlist", "Kumpulan"
   - 如果标题看起来是一个系列的一部分，标记为合格
   - 如果内容描述中提到这是系列视频的一部分，标记为合格

2. **提取频道信息**：
   - 如果在 Snippet 中看到类似 "Channel: Kok Bisa?" 的信息，优先选择

3. **不要因为没有 `list=` 参数就拒绝**：
   - Tavily 经常截断 URL
   - 只要看起来是正经教育视频，就先收录

#### 代码逻辑改进

**单集视频处理**：
```python
# 检查是否是单集视频（youtube.com/watch）
if "youtube.com/watch" in url and "list=" not in url:
    # 这是单集视频，标记为 Video Source
    video_urls.append(url)
    print(f"[📹 视频检测] 发现单集视频...")
    print(f"[📹 视频处理] 标记为 Video Source（可能是系列的一部分）")
```

**结果记录增强**：
- 在 `reason` 字段中标注资源类型（"Playlist/Channel" 或 "Video Source"）
- 区分播放列表和单集视频

#### 日志增强
- 显示 LLM 返回的 `best_indices` 和 `feedback`
- 显示发现的单集视频数量
- 显示每个资源的类型（播放列表/频道 vs 视频）

---

### 3. SearchHunter 类 - 搜索执行日志增强

#### 修改位置
`search_strategist.py` - `SearchHunter.search()` 和 `_search_ai_builders()` 方法

#### 日志增强
- 显示搜索引擎类型和最大结果数
- 显示 Tavily API 调用详情
- 显示返回结果的前3个预览
- 显示异常详情（用于调试）

---

### 4. SearchStrategist 类 - 搜索循环日志增强

#### 修改位置
`search_strategist.py` - `SearchStrategist._search_chapter()` 方法

#### 日志增强
- 每个循环开始和结束都有清晰的标记
- 显示章节信息和年级、学科
- 显示 Tavily 返回的原始结果（前5个）
- 显示每个结果的标题、URL、摘要
- 显示评估成功/失败的详细信息
- 显示资源类型统计

#### 新增功能
- 获取所有章节列表用于判断学期
- 在记录中标注资源类型（Video Source vs Playlist/Channel）

---

## 完整的日志输出示例

```
[🔍 策略] 正为章节 "Bilangan" 生成搜索词...
[🔍 策略详情] 年级: Fase A (Kelas 1-2), 学科: Matematika

============================================================
[🔄 循环 1/5] 开始新的搜索尝试
============================================================
[📝 查询生成] 尝试 1: 精准列表模式
[📝 查询详情] 限定站点: YouTube, 包含章节: bilangan
[📝 最终查询] "site:youtube.com playlist matematika kelas 1 bilangan"

[🔍 搜索执行] 调用 Tavily 搜索 API...
    [🔍 搜索] 执行搜索: "site:youtube.com playlist matematika kelas 1 bilangan"
    [🔍 搜索详情] 搜索引擎: ai-builders, 最大结果数: 10
    [🔍 搜索详情] 使用 Tavily 搜索 API
    [🔍 Tavily] 准备调用 API，查询: "site:youtube.com playlist matematika kelas 1 bilangan"
    [🔍 Tavily] 请求参数: max_results=10
    [✅ Tavily] API 调用成功，返回 10 个结果
    [📊 Tavily] 结果预览:
      [1] SMP Kelas 7 - Bilangan Bulat... -> https://www.youtube.com/playlist?list=...
      [2] BILANGAN BULAT KELAS 7... -> https://www.youtube.com/watch?v=...
      [3] Kurikulum Merdeka Matematika Kelas 7... -> https://www.youtube.com/watch?v=...

[📊 Tavily 返回结果] 共 10 个结果:
  [1] SMP Kelas 7 - Bilangan Bulat...
      URL: https://www.youtube.com/playlist?list=PL8RYU-tvzS5hoQOvt7CIeRD-4nyF5T5sD...
      摘要: Mengenal Bilangan Bulat - Matematika - Kelas 7 - SMP...
  [2] BILANGAN BULAT KELAS 7...
      URL: https://www.youtube.com/watch?v=i-x431EIdTM...
      摘要: ### Description 2128 views Posted: 2 Sep 2024...

[🕵️ LLM 评估] 开始评估搜索结果...
    [🕵️ 评估] 正在评估 10 个搜索结果...
    [🔍 评估详情] LLM 返回的 best_indices: [1, 2, 3]
    [🔍 评估详情] LLM 返回的 feedback: 找到了3个高质量资源：1个 YouTube 播放列表和2个教育平台页面...
    [📹 视频检测] 发现单集视频: https://www.youtube.com/watch?v=i-x431EIdTM...
    [📹 视频处理] 标记为 Video Source（可能是系列的一部分）
    [✅ 评估] 发现 3 个高质量资源
    [✅ 评估详情] 播放列表/频道: 1 个
    [✅ 评估详情] 单集视频（系列）: 2 个
    [✅ 资源 1] 播放列表/频道: https://www.youtube.com/playlist?list=PL8RYU-tvzS5hoQOvt7CIeRD-4nyF5T5sD...
    [✅ 资源 2] 视频（系列）: https://www.youtube.com/watch?v=i-x431EIdTM...
    [✅ 资源 3] 视频（系列）: https://www.youtube.com/watch?v=Ysjf1LtE_FM...

[✅ 评估成功] 找到 3 个高质量资源
[✅ 成功] 锁定资源 (Playlist/Channel): https://www.youtube.com/playlist?list=PL8RYU-tvzS5hoQOvt7CIeRD-4nyF5T5sD
[✅ 成功] 锁定资源 (Video Source): https://www.youtube.com/watch?v=i-x431EIdTM
[✅ 成功] 锁定资源 (Video Source): https://www.youtube.com/watch?v=Ysjf1LtE_FM
[✅ 循环完成] 在第 1 次尝试中成功找到资源，退出循环
```

---

## 预期效果

### 1. 搜索结果数量增加
- **之前**：只找到播放列表 URL，结果较少
- **现在**：包括播放列表、频道、单集视频（系列），结果更多

### 2. 搜索粒度优化
- **之前**：按章节搜索，粒度太细
- **现在**：尝试 2 去掉章节名，搜索整个学期的合集

### 3. 资源类型多样化
- **之前**：只有播放列表
- **现在**：播放列表、频道、单集视频（系列）

### 4. 日志可追溯性
- **之前**：日志较少，难以调试
- **现在**：详细的日志，每一步都有记录

---

## 测试建议

1. **小范围测试**：
   ```bash
   python3 test_small_search.py
   ```

2. **检查日志输出**：
   - 确认搜索词生成正确（特别是尝试 2）
   - 确认 Tavily 返回结果被正确解析
   - 确认 LLM 评估放宽标准生效
   - 确认单集视频被正确标记为 "Video Source"

3. **检查输出 CSV**：
   - 确认 `reason` 字段包含资源类型
   - 确认单集视频 URL 被正确保存

---

## 注意事项

1. **学期判断**：当前使用简单的关键词匹配，可能不够精确。如果章节顺序已知，可以改进。

2. **单集视频处理**：虽然放宽了标准，但单集视频可能不是完整的播放列表。建议后续手动验证。

3. **日志输出**：日志较多，可能影响性能。生产环境可以考虑减少日志或使用日志级别控制。

---

## 文件修改清单

- ✅ `search_strategist.py` - `QueryGenerator.generate_initial_query()` - 搜索策略优化
- ✅ `search_strategist.py` - `QueryGenerator._determine_semester()` - 新增学期判断方法
- ✅ `search_strategist.py` - `ResultInspector.evaluate_results()` - 评估标准放宽
- ✅ `search_strategist.py` - `ResultInspector._format_results_for_llm()` - 日志增强
- ✅ `search_strategist.py` - `SearchHunter.search()` - 日志增强
- ✅ `search_strategist.py` - `SearchHunter._search_ai_builders()` - 日志增强
- ✅ `search_strategist.py` - `SearchStrategist._search_chapter()` - 搜索循环日志增强

---

**修改完成时间**: 2025-01-XX  
**修改者**: AI Assistant

