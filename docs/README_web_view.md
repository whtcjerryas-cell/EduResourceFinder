# 网页展示系统使用说明

## 功能概述

这个系统可以：
1. 自动搜索视频播放列表（通过 `search_strategist.py`）
2. 生成美观的网页展示搜索结果（通过 `generate_web_view.py`）
3. 一键运行搜索和生成网页（通过 `run_search_and_view.py`）

## 快速开始

### 方法 1: 一键运行（推荐）

```bash
# 使用默认文件（1-2年级）
python3 run_search_and_view.py

# 或指定文件
python3 run_search_and_view.py "Knowledge Point/your_file.json"
```

### 方法 2: 分步运行

#### 步骤 1: 执行搜索

```bash
# 使用默认文件
python3 search_strategist.py

# 或指定文件
python3 search_strategist.py "Knowledge Point/your_file.json"
```

这会生成一个 CSV 文件，例如：`*_playlists.csv`

#### 步骤 2: 生成网页

```bash
# 使用默认文件
python3 generate_web_view.py

# 或指定 CSV 文件
python3 generate_web_view.py "Knowledge Point/your_file_playlists.csv"
```

这会生成一个 HTML 文件，例如：`*_playlists.html`

## 网页功能

生成的网页包含以下特性：

### 📊 统计信息
- 显示总章节数
- 显示总播放列表数

### 📚 章节分组
- 按章节自动分组播放列表
- 每个章节可展开/折叠查看详情

### 🎨 美观设计
- 现代化渐变背景
- 响应式设计，支持移动设备
- 悬停效果和动画

### 🔗 播放列表卡片
每个播放列表卡片显示：
- 播放列表 URL（可点击）
- 使用的搜索词
- 成功尝试次数

## 文件结构

```
Indonesia/
├── search_strategist.py          # 搜索脚本
├── generate_web_view.py          # 网页生成脚本
├── run_search_and_view.py        # 一键运行脚本
├── create_demo_data.py           # 创建演示数据（可选）
└── Knowledge Point/
    ├── *_knowledge_points.json   # 知识点数据
    ├── *_playlists.csv           # 搜索结果（CSV）
    └── *_playlists.html          # 网页展示（HTML）
```

## 查看网页

生成 HTML 文件后，可以通过以下方式查看：

1. **直接双击 HTML 文件**（macOS/Windows）
2. **在浏览器中打开**：
   ```bash
   open "Knowledge Point/5. Final Panduan Mata Pelajaran Matematika_12_09_2025_Revisi 3_30-58_playlists.html"
   ```
3. **复制文件路径到浏览器地址栏**：
   ```
   file:///Users/shmiwanghao8/Desktop/education/Indonesia/Knowledge Point/5. Final Panduan Mata Pelajaran Matematika_12_09_2025_Revisi 3_30-58_playlists.html
   ```

## 演示数据

如果想先查看网页效果，可以使用演示数据：

```bash
# 创建演示数据
python3 create_demo_data.py

# 生成网页
python3 generate_web_view.py
```

## 注意事项

1. **API Token**: 运行搜索需要设置 `AI_BUILDER_TOKEN` 环境变量
2. **网络连接**: 搜索过程需要网络连接
3. **时间**: 搜索可能需要较长时间，请耐心等待
4. **结果更新**: 每次运行搜索后，需要重新生成网页以查看最新结果

## 自定义样式

如果需要修改网页样式，可以编辑 `generate_web_view.py` 中的 CSS 部分。

## 故障排除

### 问题：搜索失败
- 检查 `AI_BUILDER_TOKEN` 环境变量是否设置
- 检查网络连接
- 查看错误日志

### 问题：网页无法打开
- 确保 HTML 文件路径正确
- 尝试使用绝对路径
- 检查文件权限

### 问题：网页显示为空
- 确认 CSV 文件存在且包含数据
- 检查 CSV 文件格式是否正确
- 查看控制台错误信息


