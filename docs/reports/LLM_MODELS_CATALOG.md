# LLM大模型使用清单

**项目**: 全教育层级智能搜索系统
**版本**: v5.0
**更新日期**: 2026-01-06

---

## 📋 目录

1. [系统架构](#系统架构)
2. [使用的大模型](#使用的大模型)
3. [模型配置文件](#模型配置文件)
4. [使用场景分布](#使用场景分布)
5. [API调用优先级](#api调用优先级)
6. [降级策略](#降级策略)

---

## 系统架构

### 双API系统架构

```
┌──────────────────────────────────────────────────────────┐
│                  LLM调用架构                              │
└──────────────────────────────────────────────────────────┘

┌──────────────────────┐
│  应用层              │
│  - discovery_agent   │
│  - search_engine     │
│  - video_evaluator   │
│  - search_strategist │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│  统一LLM客户端       │
│  (llm_client.py)     │
└──────────┬───────────┘
           │
      ┌────┴────┐
      │         │
      ▼         ▼
┌────────────────┐  ┌──────────────────┐
│ 公司内部API     │  │ AI Builders API  │
│ (优先使用)      │  │ (备用系统)       │
└────────────────┘  └──────────────────┘
```

---

## 使用的大模型

### 🏢 公司内部API模型（优先使用）

**API基础地址**:
- 生产环境: `https://hk-intra-paas.transsion.com/tranai-proxy/v1`
- 测试环境: `https://uat-intra-paas.transsion.com/tranai-proxy/v1`

**支持模型列表**:

| 模型名称 | 用途 | 响应时间 | 特点 |
|---------|------|---------|------|
| **gpt-4o** | 默认文本生成 | ~3-5秒 | 高质量、多语言支持 |
| **gemini-2.5-flash** | 快速推理/视觉 | ~2-3秒 | 速度快、高质量 |
| **gemini-2.5-pro** | 高质量推理 | ~5-8秒 | 推理能力强 |
| **gpt-4o-mini** | 批量分类 | ~2秒 | 低成本、高速度 |

#### 1. gpt-4o

**配置位置**: `config/llm.yaml` Line 14
```yaml
internal_api: "gpt-4o"
```

**使用场景**:
- ✅ 国家教育体系AI调研（`discovery_agent.py`）
- ✅ 年级-学科配对信息提取
- ✅ K12教育搜索查询生成

**特性**:
- 多语言支持（印尼语、中文、英语）
- 高质量文本生成
- JSON输出格式化能力强

---

#### 2. gemini-2.5-flash

**配置位置**: `config/llm.yaml` Line 18, 22
```yaml
fast_inference: "gemini-2.5-flash"
vision: "gemini-2.5-flash"
```

**使用场景**:
- ✅ **快速推理**: 搜索推荐理由生成（2-3秒）
- ✅ **视觉分析**: 视频深度评估
- ✅ 搜索策略生成

**特性**:
- ⚡ 极快响应速度（2-3秒）
- 👁️ 支持视觉分析（理解视频缩略图）
- 💰 性价比高
- 🎯 高质量输出

**视觉分析示例**:
```python
# 视频评估时使用
vision_client = VisionClient()  # 使用gemini-2.5-flash
result = vision_client.analyze_images(
    image_paths=["thumbnail1.jpg", "thumbnail2.jpg"],
    prompt="分析这个教育视频的质量"
)
```

---

#### 3. gemini-2.5-pro

**配置位置**: `config/llm.yaml` Line 40
```yaml
gemini: "gemini-2.5-pro"
```

**使用场景**:
- ✅ 高质量推理任务
- ✅ 复杂逻辑分析
- ✅ 作为备用模型（降级使用）

**特性**:
- 🧠 强大的推理能力
- 📊 支持长文本（8000 tokens）
- ⚖️ 适合复杂决策

---

#### 4. gpt-4o-mini

**配置位置**: `config/llm.yaml` Line 28
```yaml
classification: "gpt-4o-mini"
```

**使用场景**:
- ✅ 批量分类任务
- ✅ 简单文本处理
- ✅ 成本敏感型任务

**特性**:
- 💰 极低成本
- ⚡ 高速度（~2秒）
- 📦 适合批量处理

---

### 🌐 AI Builders API模型（备用系统）

**API基础地址**: `https://space.ai-builders.com/backend`

**支持模型列表**:

| 模型名称 | 用途 | 响应时间 | 特点 |
|---------|------|---------|------|
| **grok-4-fast** | 默认备用 | ~10秒 | 已验证可用 |
| **deepseek** | 搜索策略 | ~15秒 | 稳定可靠 |
| **gemini-2.5-pro** | 高质量 | ~20秒 | 通过AI Builders访问 |

#### 5. grok-4-fast

**配置位置**: `config/llm.yaml` Line 31
```yaml
ai_builders_default: "grok-4-fast"  # 10秒响应，已验证可用
```

**使用场景**:
- ✅ 公司内部API不可用时的备用
- ✅ 通用文本生成
- ✅ 紧急降级场景

**特性**:
- ✅ 已验证可用
- ⏱️ 10秒响应时间
- 🔄 稳定可靠

---

#### 6. deepseek

**配置位置**: `config/llm.yaml` Line 25
```yaml
search_strategy: "deepseek"  # 使用AI Builders的deepseek（稳定）
```

**使用场景**:
- ✅ 搜索策略生成（`search_strategy_agent.py`）
- ✅ 公司内部API不可用时的降级
- ✅ 高推理需求任务

**特性**:
- 🧠 强大的推理能力
- 💰 低成本
- ✨ 数学、逻辑问题处理强

**使用示例**:
```python
# 搜索策略生成
agent = SearchStrategyAgent()
strategy = agent.generate_search_strategy(
    country="ID",
    grade="Kelas 1",
    subject="Matematika",
    query="Pecahan"
)
# 调用deepseek生成优化后的搜索词
```

---

### 🎤 其他专业模型

#### 7. Whisper (语音转录)

**配置位置**: `config/llm.yaml` Line 43
```yaml
transcription: "base"
```

**使用场景**:
- ✅ 视频字幕提取
- ✅ 语音转文字

**模型规格**:
- tiny: 最快，准确率较低
- **base**: 平衡（默认）✅
- small: 较准确
- medium: 更准确
- large: 最准确，最慢

**使用位置**: `core/transcript_extractor.py`

---

## 模型配置文件

### 主配置文件: `config/llm.yaml`

**完整模型配置**:
```yaml
llm:
  models:
    # 内部API默认模型
    internal_api: "gpt-4o"

    # 快速推理模型
    fast_inference: "gemini-2.5-flash"

    # 视觉分析模型
    vision: "gemini-2.5-flash"

    # 搜索策略生成模型
    search_strategy: "deepseek"

    # 批量分类模型
    classification: "gpt-4o-mini"

    # AI Builders默认模型
    ai_builders_default: "grok-4-fast"

    # 备用模型降级顺序
    fallback_order:
      - "gemini-2.5-flash"
      - "grok-4-fast"
      - "deepseek"

    # Gemini模型
    gemini: "gemini-2.5-pro"

    # Whisper转录模型
    transcription: "base"
```

---

## 使用场景分布

### 场景1: K12教育搜索

**涉及模型**:
- **gpt-4o**: 国家教育体系调研（一次性）
- **gemini-2.5-flash**: 搜索推荐理由生成（每次搜索）
- **deepseek** (备用): 搜索策略优化

**示例流程**:
```
1. AI调研国家教育体系 → gpt-4o
2. 用户发起搜索
3. 生成搜索策略 → deepseek (可选)
4. 执行搜索
5. 生成推荐理由 → gemini-2.5-flash
```

---

### 场景2: 视频深度评估

**涉及模型**:
- **gemini-2.5-flash**: 视觉分析（理解缩略图）

**示例代码**:
```python
from core.vision_client import VisionClient

vision_client = VisionClient()  # 使用gemini-2.5-flash
result = vision_client.analyze_images(
    image_paths=["video_thumbnail.jpg"],
    prompt="评估这个教育视频的教学质量和适用年级"
)
```

---

### 场景3: 搜索策略生成

**涉及模型**:
- **deepseek**: 生成优化后的搜索查询

**示例代码**:
```python
from search_strategy_agent import SearchStrategyAgent

agent = SearchStrategyAgent()
strategy = agent.generate_search_strategy(
    query="Pecahan",
    context="印尼一年级数学"
)
# deepseek生成: "Matematika Pecahan Kelas 1 SD Indonesia"
```

---

### 场景4: 大学/职业教育搜索

**涉及模型**:
- **gpt-4o**: 生成大学/职业相关查询
- **gemini-2.5-flash**: 快速推理

**查询生成示例**:
```
原始: "Algoritma"
↓ (gpt-4o处理)
优化后: "Teknik Informatika Fakultas Ilmu Komputer
        Universitas Indonesia Algoritma dan Pemrograman"
```

---

## API调用优先级

### 优先级规则

```
┌────────────────────────────────────────┐
│  1. 公司内部API（优先）                 │
│     - gpt-4o                           │
│     - gemini-2.5-flash                 │
│     - gemini-2.5-pro                   │
│                                        │
│  2. AI Builders API（备用）            │
│     - grok-4-fast                      │
│     - deepseek                         │
│     - gemini-2.5-pro                   │
└────────────────────────────────────────┘
```

### 客户端实现

**文件**: `llm_client.py`

```python
class UnifiedLLMClient:
    def __init__(self):
        # 1. 尝试初始化公司内部API
        try:
            self.internal_client = InternalAPIClient()
            self.primary_api = "internal"
        except:
            self.primary_api = "ai_builders"

        # 2. 始终初始化AI Builders API作为备用
        self.ai_builders_client = AIBuildersAPIClient()

    def call_llm(self, prompt, system_prompt=None):
        # 优先使用公司内部API
        if self.primary_api == "internal":
            try:
                return self.internal_client.call_llm(prompt, system_prompt)
            except Exception as e:
                print(f"[⚠️] 公司内部API失败，切换到AI Builders")
                # 降级到AI Builders
                return self.ai_builders_client.call_llm(prompt, system_prompt)
        else:
            # 直接使用AI Builders
            return self.ai_builders_client.call_llm(prompt, system_prompt)
```

---

## 降级策略

### 自动降级流程

```
┌────────────────────────────────────────────────┐
│  开始调用                                      │
└──────────┬───────────────────────────────────┘
           │
           ▼
┌──────────────────────┐
│ 尝试: gpt-4o         │
│ (公司内部API)        │
└──────────┬───────────┘
           │
      失败?
           │
           ▼
┌──────────────────────┐
│ 降级: gemini-2.5-flash│
│ (公司内部API)        │
└──────────┬───────────┘
           │
      失败?
           │
           ▼
┌──────────────────────┐
│ 降级: grok-4-fast    │
│ (AI Builders API)    │
└──────────┬───────────┘
           │
      失败?
           │
           ▼
┌──────────────────────┐
│ 降级: deepseek       │
│ (AI Builders API)    │
└──────────────────────┘
```

### 降级配置

**文件**: `config/llm.yaml` Line 34-37
```yaml
fallback_order:
  - "gemini-2.5-flash"    # 公司内部API
  - "grok-4-fast"         # AI Builders
  - "deepseek"            # AI Builders
```

---

## 模型对比表

### 性能对比

| 模型 | API | 响应时间 | 推理能力 | 视觉支持 | 成本 | 主要用途 |
|------|-----|---------|---------|---------|------|---------|
| **gpt-4o** | 内部 | 3-5秒 | ⭐⭐⭐⭐⭐ | ❌ | 高 | 文本生成 |
| **gemini-2.5-flash** | 内部 | 2-3秒 | ⭐⭐⭐⭐ | ✅ | 中 | 快速推理+视觉 |
| **gemini-2.5-pro** | 内部 | 5-8秒 | ⭐⭐⭐⭐⭐ | ❌ | 高 | 复杂推理 |
| **gpt-4o-mini** | 内部 | 2秒 | ⭐⭐⭐ | ❌ | 低 | 批量分类 |
| **grok-4-fast** | AI Builders | 10秒 | ⭐⭐⭐ | ❌ | 中 | 备用 |
| **deepseek** | AI Builders | 15秒 | ⭐⭐⭐⭐ | ❌ | 低 | 搜索策略 |
| **Whisper base** | 本地 | - | - | ❌ | - | 语音转录 |

### 推荐使用场景

**追求速度**: gemini-2.5-flash
**追求质量**: gpt-4o 或 gemini-2.5-pro
**追求性价比**: deepseek
**视觉分析**: gemini-2.5-flash
**批量处理**: gpt-4o-mini

---

## 环境变量配置

### 必需的环境变量

创建 `.env` 文件：

```bash
# 公司内部API（优先）
INTERNAL_API_KEY=your_internal_api_key_here
INTERNAL_API_BASE_URL=https://hk-intra-paas.transsion.com/tranai-proxy/v1

# AI Builders API（备用）
AI_BUILDER_TOKEN=your_ai_builders_token_here
```

---

## 使用统计

### 典型搜索请求的模型调用

**K12搜索（单次）**:
1. **可选**: deepseek - 搜索策略优化（1次）
2. **必须**: gemini-2.5-flash - 推荐理由生成（3-5次）
3. **可选**: gemini-2.5-flash - 视觉评估（如果评估视频）

**总API调用**: 1-6次/搜索

**国家调研（一次性）**:
1. **gpt-4o** - 教育体系信息提取（3-5次，每轮验证）

---

## 优化建议

### 1. 成本优化

- ✅ 使用`gemini-2.5-flash`替代`gpt-4o`（降低50%成本）
- ✅ 启用缓存（避免重复调用）
- ✅ 批量处理使用`gpt-4o-mini`

### 2. 性能优化

- ✅ 优先使用`gemini-2.5-flash`（2-3秒响应）
- ✅ 并行调用多个API
- ✅ 设置合理的超时（60秒）

### 3. 稳定性优化

- ✅ 实施完整的降级策略
- ✅ 重试机制（最多3次）
- ✅ 双API系统（内网 + 外网）

---

## 总结

### 使用的LLM大模型总数: 7个

**公司内部API（4个）**:
1. gpt-4o
2. gemini-2.5-flash
3. gemini-2.5-pro
4. gpt-4o-mini

**AI Builders API（2个）**:
5. grok-4-fast
6. deepseek

**专业模型（1个）**:
7. Whisper (base)

### 核心特性

- ✅ **双API系统**: 公司内部（优先）+ AI Builders（备用）
- ✅ **自动降级**: 主模型失败自动切换备用模型
- ✅ **视觉支持**: gemini-2.5-flash理解视频缩略图
- ✅ **多语言**: 所有模型支持印尼语、中文、英语
- ✅ **灵活配置**: 通过`config/llm.yaml`统一管理

### 推荐配置

**生产环境**:
```yaml
primary: gemini-2.5-flash  # 速度快、质量好
vision: gemini-2.5-flash    # 视觉分析
backup: grok-4-fast         # 稳定备用
strategy: deepseek          # 搜索策略
```

---

**文档版本**: v1.0
**最后更新**: 2026-01-06
**维护团队**: Education Search System Dev Team
