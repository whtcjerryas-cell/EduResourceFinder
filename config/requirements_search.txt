# 印尼 K12 视频课程库搜索 Agent 依赖项

# 核心依赖
pydantic>=2.0.0
requests>=2.31.0
python-dotenv>=1.0.0  # 支持从 .env 文件读取环境变量

# 搜索引擎（二选一）
duckduckgo-search>=4.0.0  # 推荐：免费，无需API密钥
# serpapi>=0.1.5  # 可选：需要API密钥，更稳定

# 可选：重试库（代码中已实现重试逻辑，但可以使用此库增强）
# tenacity>=8.2.0

